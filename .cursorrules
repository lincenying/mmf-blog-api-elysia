alwaysApply: true

Cursor 开发规范（Elysia.js + Vue3 + Drizzle ORM 技术栈）

0. 技术栈

- 运行时: Bun
- 后端框架: Elysia.js (基于Bun的Web框架)
- 前端框架: Vue3 + TypeScript
- UI 组件库: Element Plus（管理端表单、表格、对话框、导航等）
- 数据库: SQLite (开发环境) / PostgreSQL‌ (生产环境)
- ORM: Drizzle ORM
- 样式: Element Plus 主题与组件样式；UnoCSS 作少量辅助原子类（可选）
- 身份验证: JWT + Cookie
- 部署: docker

1. 技术栈统一规范
1.1 运行时与后端
- 运行时：Bun（禁止使用 Node.js 独有 API，依赖包必须兼容 Bun）
- 后端框架：Elysia.js（严格遵循 Elysia 官方插件化、类型安全写法）
- 身份验证：JWT + HttpOnly Cookie（前端禁止直接操作 Token，仅后端读写 Cookie）

1.2 前端
- 框架：Vue3 + TypeScript（仅使用 `<script setup>` 语法）
- UI 库：Element Plus（优先使用内置组件，禁止随意引入第三方 UI）
- 样式：Element Plus 主题优先 + UnoCSS 原子类（仅做辅助，禁止过度使用）
- 工程规范：组合式 API + 类型定义优先，禁止使用 Options API

1.3 数据库与 ORM
- 开发环境：SQLite
- 生产环境：PostgreSQL
- ORM：Drizzle ORM（统一使用 Drizzle 语法，禁止手写原生 SQL）

2. 项目结构规范（强制目录约定）
2.1 后端目录（Elysia.js）
backend/
├── src/
│   ├── config/         # 配置文件（JWT、数据库、环境变量）
│   ├── db/             # Drizzle 相关
│   │   ├── schema/     # 数据表定义（按模块拆分）
│   │   └── index.ts    # Drizzle 实例初始化
│   ├── modules/        # 业务模块（按功能拆分，如 user、auth、admin）
│   │   ├── module.controller.ts  # 路由控制器
│   │   ├── module.service.ts     # 业务逻辑
│   │   ├── module.schema.ts      # 请求/响应校验（Elysia Schema）
│   │   └── module.types.ts       # TypeScript 类型定义
│   ├── plugins/        # Elysia 插件（JWT、CORS、权限、日志）
│   ├── utils/          # 工具函数
│   ├── app.ts          # Elysia 应用入口
│   └── index.ts        # 服务启动文件
├── .env.development    # 开发环境变量
├── .env.production     # 生产环境变量
└── drizzle.config.ts   # Drizzle 配置

2.2 前端目录（Vue3 + TypeScript）
frontend/
├── src/
│   ├── api/            # API 请求封装（按模块拆分）
│   ├── assets/         # 静态资源
│   ├── components/     # 公共组件（Element Plus 二次封装）
│   ├── composables/    # 组合式函数
│   ├── layouts/        # 布局组件（管理端布局）
│   ├── pages/          # 页面组件（按路由拆分）
│   ├── router/         # Vue Router 路由配置
│   ├── stores/         # 状态管理
│   ├── types/          # 全局 TS 类型
│   ├── utils/          # 工具函数
│   ├── App.vue
│   └── main.ts
├── .env.development
└── .env.production


3. 代码风格与语法规范
3.1 通用规范
- 语言：TypeScript 优先，所有文件必须添加类型定义，禁止使用 `any`（特殊场景需标注注释）
- 命名规范：
    - 文件命名：kebab-case（如 `user.controller.ts`、`login-page.vue`）
    - 变量/函数：camelCase
    - 类/类型/接口：PascalCase
    - 常量：UPPER_SNAKE_CASE
- 格式化：统一使用 Prettier，遵循项目内 `.prettierrc` 配置

3.2 后端（Elysia.js）规范
1. 插件化开发：所有业务逻辑必须拆分为 Elysia 插件，禁止在主文件编写业务代码
2. 类型安全：使用 `Elysia.Schema` 校验所有请求参数、响应数据，自动生成 OpenAPI 文档
3. 分层架构：严格遵循 `Controller → Service → DB` 分层
    - Controller：仅处理路由、参数接收、响应返回
    - Service：处理核心业务逻辑、权限校验、数据组装
4. Drizzle 规范
    - 数据表结构统一在 `db/schema/` 定义，支持 SQLite/PostgreSQL 兼容
    - 所有数据库操作必须通过 Drizzle 实例，禁止原生 SQL
5. 身份验证
    - JWT 密钥、过期时间统一在 `config/` 管理
    - 登录成功后由后端写入 HttpOnly Cookie，前端无法获取
    - 接口权限通过 Elysia 插件全局校验，禁止每个接口重复校验

3.3 前端（Vue3 + Element Plus）规范
1. 语法强制：仅使用 `<script setup lang="ts">`，禁止 Options API
2. Element Plus 使用
    - 管理端表格、表单、弹窗、分页、导航必须使用 Element Plus 组件
    - 表单校验使用 Element Plus 内置规则，禁止自定义复杂校验
    - 主题定制统一在 `styles/element-theme.scss` 修改，禁止组件内覆盖样式
3. API 封装
    - 所有接口统一在 `api/` 目录封装，返回 Promise + 类型定义
    - 请求/响应拦截器统一处理 Token、错误提示、登录过期
4. 样式规范
    - 优先使用 Element Plus 内置样式类
    - 简单布局、间距使用 UnoCSS，禁止编写冗余 CSS
5. 类型规范
    - 接口请求/响应类型统一放在 `types/` 目录
    - Props 必须使用 `defineProps<{ 类型 }>()` 定义

4. 接口与数据交互规范
4.1 响应格式（前后端统一）
```typescript
// 全局统一响应类型
interface ApiResponse<T = any> {
    code: number // 200 成功，401 未授权，500 服务器错误
    message: string // 提示信息
    data: T // 业务数据
}
```

4.2 身份验证流程
前端提交账号密码 → 后端校验 → 生成 JWT → 写入 HttpOnly Cookie
前端后续请求自动携带 Cookie，后端插件自动校验 JWT
登出：后端清除 Cookie，前端清空本地状态
未授权自动跳转登录页（前端路由守卫 + 后端 401 响应）

4.3 跨域处理
后端通过 Elysia CORS 插件配置，允许前端域名
禁止前端使用代理解决跨域（生产环境禁用）

5. 环境变量与配置规范
环境文件：.env.development / .env.production，禁止提交敏感信息到 Git
后端配置：数据库连接、JWT 密钥、端口仅从环境变量读取
前端配置：接口地址、静态资源域名从环境变量读取
环境变量命名：VITE_*（前端）、无前缀（后端）

6. Git 提交信息规范
You MUST always generate git commit messages in Simplified Chinese (简体中文).
DO NOT use English for the description.

Format: Conventional Commits
- feat: 新功能
- fix: 修补错误
- docs: 文档改变
- style: 代码格式改变
- refactor: 某个已有功能重构
- perf: 性能优化
- test: 增加测试
- build: 改变构建流程
- ci: 改变持续集成配置
- chore: 辅助工具的变动

Examples:
- feat: 完善用户登录逻辑
- fix: 解决诗词详情页加载异常
- docs: 更新 README 文件

示例：feat(backend): add user login api

7. 禁止行为（强制遵守）
禁止后端使用 Node.js 独有 API，必须兼容 Bun
禁止前端直接操作 JWT Token，必须通过 Cookie 传输
禁止手写原生 SQL，必须使用 Drizzle ORM
禁止在 Vue3 中使用 Options API
禁止随意引入第三方依赖，需评估兼容性
禁止使用 any 类型，必须定义完整 TS 类型
禁止业务代码写在入口文件，必须按模块拆分
